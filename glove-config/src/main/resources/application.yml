# config server port
server:
  port: ${CONFIG_SERVER_PORT:8888}

# config server name
spring:
  application:
    name: config-server
  profiles:
    active: native
  cloud:
    config:
      server:
        native:
          search-locations: classpath:config/
